# Routine completion log

Every routine appends one row per run. Newest at the bottom. Do not edit historical rows; they are an audit trail.

| Date/time (IST) | Routine | Trigger | Branch | PR | Status | Summary |
|-----------------|---------|---------|--------|-----|--------|---------|
| 2026-04-20 09:00 | _example_ | scheduled | routine/fix-accessibility/2026-04-20-alt-text | _link_ | merged | Added alt text to 12 images across /products and /industries routes |
| 2026-04-21 08:05 IST | fix-security | scheduled | routine/fix-security/2026-04-21-headers | https://github.com/mobiliseapplabllp/robotics-website/compare/main...routine/fix-security/2026-04-21-headers | pr-open: awaiting review | Scan clean on code/deps — `npm audit` 0 vulns (513 deps); `npm outdated` shows only major-version drift (react 18→19, vite 6→8, typescript 5→6, eslint 9→10, etc.) — all escalate-only per GUARDRAILS, none bumped this run. Secret scan fell back to manual regex grep (gitleaks/trufflehog not on PATH per memory) — 0 hits for API_KEY/SECRET/AWS/GH/GOOGLE/OpenAI/Slack token patterns in `src/`. No `http://` in src, no open CORS, no `eval(`/`new Function(`. One pre-existing `dangerouslySetInnerHTML` in `src/app/components/product/MobiliseAuthoritySection.tsx:120` — verified all callers (C20/C30/C40/C55/T3/T5/T8/T9/T9Pro/T10/T11/W3/S100/S300/G1 pages) pass literal strings or no prop, so no live XSS vector; flagged as follow-up. FIX (auto-merge-eligible per routine STEP 2): added `Strict-Transport-Security: max-age=31536000; includeSubDomains`, `Cross-Origin-Opener-Policy: same-origin`, `Cross-Origin-Resource-Policy: same-origin`, and `Content-Security-Policy-Report-Only` (baseline: default-src 'self'; script/style-src 'self' 'unsafe-inline' to cover inline JSON-LD + Tailwind/motion inline styles; img-src 'self' data: blob: https:; frame-ancestors 'none'; object-src 'none'; base-uri + form-action 'self') to BOTH `vercel.json` AND `render.yaml` (dual-config per memory). CSP started in Report-Only per routine guidance — promote after ~1 week of clean reports. Also expanded Permissions-Policy to deny payment/usb/magnetometer/gyroscope/accelerometer alongside existing camera/mic/geo. Verification: typecheck ✓ / lint ✓ / test:run (4/4) ✓ / build ✓ / vercel.json JSON.parse ok. 2 files, +21/-2 — well under 40-file/800-line cap. NOT merged autonomously: no CI workflows exist in repo (no `.github/workflows/`) so GUARDRAILS "merge only when all required status checks are green" reduces to a no-check state — safer to leave PR open for human review before letting Vercel/Render auto-deploy on main push. Compare URL in "PR" column for reviewer. |
| 2026-05-08 03:20 IST | publish-industry-blog | scheduled | routine/publish-industry-blog/2026-05-08-qsr-server-robot (pushed to origin, tip 131db683) | https://github.com/mobiliseapplabllp/robotics-website/compare/main...routine/publish-industry-blog/2026-05-08-qsr-server-robot | pr-open: awaiting content review | Phase 1 row 3 — Restaurants (QSR chains) × DINERBOT × labour-shortage ROI; primary keyword "restaurant server robot india". Skip log: Row 1 "room service robot india" retro-marked `shipped` because `src/content/blog/posts/hotel-room-service-robot-roi.md` (seed post, Apr 2026) covers the same industry × robot type × angle — publishing a near-duplicate would cannibalise. Row 2 "hotel concierge robot india" set to `blocked-source-verification` because no specific KEENON guiding-robot model is whitelisted in `BRAND.md`; STEP 4 requires named models. Drafted `src/content/blog/posts/qsr-server-robot-india.md` (~2,000 body words, 11-min read), author Rohan Kapoor, category Industry, tags QSR/Hospitality/ROI/Labour/India. Structure follows BLOG-TEMPLATE.md as closely as the current `BlogPost.tsx` allows: definitional first sentence, plain-paragraph Quick Answer (75 words, primary keyword in first sentence), inline blockquote CTA, six required H2s (why-ready / what-it-does / ROI / risks / procurement / MALL difference), 8 inline H3 FAQs (FAQPage schema deferred per memory `Blog template vs component gap` — current component only emits BlogPosting JSON-LD), italic sign-off. Five `[HUMAN INPUT REQUIRED]` markers preserved (KEENON video embed swap-in; canonical-scenario replacement with real pilot; pilot/case-study paragraph; current BCD rate after CHA confirmation; original VIDEO-BACKLOG entry). One `[ASSET MISSING]` flag for `qsr-roi.pdf` (BLOG-ASSETS.md status TODO). Citations all from BLOG-SOURCES.md whitelist (NRAI, IBEF, FSSAI, GST portal, DGFT, KEENON.com); homepage URLs used where exact-report URLs are not stable, with phrasing that flags claims as industry-report-grade. INR cost bands grounded in 2026 single-unit quote experience; chain pricing left to discovery-call. Banned-phrases manual grep (BLOG-STYLE.md table + BRAND.md off-limits superlatives "world-first / leading / best / only / India's first") — single hit "Best for" column header rewritten to "Suits". India-English check (centre/metre/colour/behaviour/etc.) clean. Verification: typecheck ✓ / lint ✓ / test:run (8/8) ✓ / build ✓ in 19.65s. Verification scripts called for in routine STEP 7 (`check:links`, `check:schema`, `lighthouse`, `readability`, `banned-phrases`, `ai-detection`) do not exist per memory `Blog verification scripts not wired` — fell back to typecheck/lint/build/test:run + manual banned-phrase grep. Diff: 4 files, +254/-3 (1 new blog post + scratchpad research note + 2 doc updates) — well under 25-file blog cap and 800-line cap. NOT merged autonomously: per GUARDRAILS clause "Any new blog post (the publish-industry-blog routine is PR-only by design)" + memory `Routine is PR-only`; PR-only is binding. Compare URL in "PR" column for reviewer. Slack alert for `#site-content` not posted — no Slack integration is wired in this repo; reviewer to ping channel manually. |
