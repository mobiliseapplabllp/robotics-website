# Routine completion log

Every routine appends one row per run. Newest at the bottom. Do not edit historical rows; they are an audit trail.

| Date/time (IST) | Routine | Trigger | Branch | PR | Status | Summary |
|-----------------|---------|---------|--------|-----|--------|---------|
| 2026-04-20 09:00 | _example_ | scheduled | routine/fix-accessibility/2026-04-20-alt-text | _link_ | merged | Added alt text to 12 images across /products and /industries routes |
| 2026-04-21 08:05 IST | fix-security | scheduled | routine/fix-security/2026-04-21-headers | https://github.com/mobiliseapplabllp/robotics-website/compare/main...routine/fix-security/2026-04-21-headers | pr-open: awaiting review | Scan clean on code/deps — `npm audit` 0 vulns (513 deps); `npm outdated` shows only major-version drift (react 18→19, vite 6→8, typescript 5→6, eslint 9→10, etc.) — all escalate-only per GUARDRAILS, none bumped this run. Secret scan fell back to manual regex grep (gitleaks/trufflehog not on PATH per memory) — 0 hits for API_KEY/SECRET/AWS/GH/GOOGLE/OpenAI/Slack token patterns in `src/`. No `http://` in src, no open CORS, no `eval(`/`new Function(`. One pre-existing `dangerouslySetInnerHTML` in `src/app/components/product/MobiliseAuthoritySection.tsx:120` — verified all callers (C20/C30/C40/C55/T3/T5/T8/T9/T9Pro/T10/T11/W3/S100/S300/G1 pages) pass literal strings or no prop, so no live XSS vector; flagged as follow-up. FIX (auto-merge-eligible per routine STEP 2): added `Strict-Transport-Security: max-age=31536000; includeSubDomains`, `Cross-Origin-Opener-Policy: same-origin`, `Cross-Origin-Resource-Policy: same-origin`, and `Content-Security-Policy-Report-Only` (baseline: default-src 'self'; script/style-src 'self' 'unsafe-inline' to cover inline JSON-LD + Tailwind/motion inline styles; img-src 'self' data: blob: https:; frame-ancestors 'none'; object-src 'none'; base-uri + form-action 'self') to BOTH `vercel.json` AND `render.yaml` (dual-config per memory). CSP started in Report-Only per routine guidance — promote after ~1 week of clean reports. Also expanded Permissions-Policy to deny payment/usb/magnetometer/gyroscope/accelerometer alongside existing camera/mic/geo. Verification: typecheck ✓ / lint ✓ / test:run (4/4) ✓ / build ✓ / vercel.json JSON.parse ok. 2 files, +21/-2 — well under 40-file/800-line cap. NOT merged autonomously: no CI workflows exist in repo (no `.github/workflows/`) so GUARDRAILS "merge only when all required status checks are green" reduces to a no-check state — safer to leave PR open for human review before letting Vercel/Render auto-deploy on main push. Compare URL in "PR" column for reviewer. |
| 2026-05-21 07:45 IST | fix-security | scheduled | routine/fix-security/2026-05-21-ws-patch | https://github.com/mobiliseapplabllp/robotics-website/compare/main...routine/fix-security/2026-05-21-ws-patch | pr-open: awaiting review | Scan: `npm audit` surfaced 1 moderate (CVSS 4.4) — `ws@8.20.0` uninitialized-memory disclosure (GHSA-58qx-3vcg-4xpx, CWE-908), transitive devDependency via `jsdom@25.0.1`, not in browser bundle. `fixAvailable: true`. `npm outdated`: 0 within-semver upgrades — all "outdated" entries are major bumps (react 18→19, vite 6→8, typescript 5→6, eslint 9→10, jsdom 25→29, @types/node 22→25, @types/react 18→19, @vitejs/plugin-react 4→6, eslint-plugin-react-hooks 5→7, etc.), all escalate-only per GUARDRAILS — none touched. Secret scan: gitleaks/trufflehog not on PATH per memory `reference_secret_scanners_missing.md`; manual regex grep clean for AKIA/sk_live/xox[bpoa]/ghp_/gho_/AIzaSy/sk-/PEM private keys and generic api_key/secret/password/token assignments — 0 hits in repo. Code-pattern scan in `src/`: no `dangerouslySetInnerHTML` (prior MobiliseAuthoritySection occurrence removed in `refactor/authority-section-no-dangerous-html`, now on main), no `eval(`, no `new Function(`, no `http://` (only false positives in sitemap xmlns + favicon xmlns + routine prompt text under repo root), no CORS config / Access-Control-Allow-Origin (static site, no API surface), no backend POST endpoints (Contact uses mailto:, NewsletterSignup is a client-side stub — no rate-limit gap). Headers: production DNS `mobiliserobotics.com` NXDOMAIN from this host (site not yet live or DNS not propagated) — live HEAD probe unavailable; source-of-truth configs verified intact (full suite from 2026-04-21 PR is on main: HSTS, COOP, CORP, XCTO, XFO, RP, Permissions-Policy denying camera/mic/geo/payment/usb/magnetometer/gyroscope/accelerometer, CSP-Report-Only). FIX: lockfile-only patch bump `ws 8.20.0 → 8.20.1` via `npm audit fix` (no package.json change). Verification: `npm audit` clean (0 vulns post-fix) / typecheck ✓ / lint ✓ / test:run 8/8 ✓ / build ✓ in 9.16s. 1 file, +3/-3 — well under 40-file/800-line cap. NOT merged autonomously: per routine STEP 2 only `high`/`critical` audit fixes are explicitly auto-merge-eligible (this is moderate); also no CI workflows exist in repo so "merge only when all required status checks are green" reduces to a no-check state — safer to leave for human review before Vercel/Render auto-deploys on main push. Live-deploy gate also blocked by domain not resolving — flag for ops review. Compare URL in "PR" column. |
